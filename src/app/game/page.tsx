'use client';nnimport { useSession } from 'next-auth/react';nimport { useRouter } from 'next/navigation';nimport { useEffect } from 'react';nnexport default function GamePage() {n  const { data: session, status } = useSession();n  const router = useRouter();nn  useEffect(() => {n    if (status === 'loading') return;n    n    if (!session) {n      router.push('/');n    }n  }, [session, status, router]);nn  if (status === 'loading') {n    return (n      <div className="min-h-screen flex items-center justify-center bg-gray-50">n        <div className="text-center">n          <div className="animate-spin rounded-full h-12 w-12 border-b-2 border-indigo-600 mx-auto"></div>n          <p className="mt-4 text-gray-600">Loading...</p>n        </div>n      </div>n    );n  }nn  if (!session) {n    return null;n  }nn  return (n    <div className="min-h-screen bg-gray-900 text-white p-8">n      <div className="max-w-4xl mx-auto">n        <h1 className="text-4xl font-bold mb-8">Dragon Court</h1>n        <p className="text-xl mb-4">Welcome, {session.user?.email}!</p>n        <p className="text-gray-300">The game is coming soon...</p>n      </div>n    </div>n  );n}