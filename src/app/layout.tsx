import type { Metadata } from "next";nimport "./globals.css";nimport Providers from "@/components/Providers";nnexport const metadata: Metadata = {n  title: "Dragon Court",n  description: "It's back.",n};nnexport default function RootLayout({n  children,n}: {n  children: React.ReactNoden}) {n  return (n    <html lang="en">n      <body>n        <Providers>n          {children}n        </Providers>n      </body>n    </html>n  )n}